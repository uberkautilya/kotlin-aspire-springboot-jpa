spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/learn_kotlin
    username: username
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update